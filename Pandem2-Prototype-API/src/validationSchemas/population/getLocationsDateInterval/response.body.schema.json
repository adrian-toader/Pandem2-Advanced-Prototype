{
  "$id": "population.getLocationsDateInterval.response.body.schema",
  "description": "Get time interval for populations for requested locations response body schema",
  "type": "object",
  "properties": {
    "start_date": {
      "type": "string",
      "format": "date"
    },
    "end_date": {
      "type": "string",
      "format": "date"
    }
  }
}
