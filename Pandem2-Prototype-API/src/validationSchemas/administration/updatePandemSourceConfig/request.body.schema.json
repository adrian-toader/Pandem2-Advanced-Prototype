{
  "$id": "administration.updatePandemSourceConfig.request.body.schema",
  "description": "Update the pandemSource object in the database",
  "type": "object",
  "properties": {
    "url": {
      "$ref": "/base#/definitions/url"
    },
    "requestTimeout": {
      "type": "number",
      "default": 60000
    }
  },
  "required": [
    "url",
    "requestTimeout"
  ]
}