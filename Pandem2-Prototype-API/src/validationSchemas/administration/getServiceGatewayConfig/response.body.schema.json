{
  "$id": "administration.getServiceGatewayConfig.response.body.schema",
  "description": "Retrieve the serviceGateway object from the database, or if it is not found, retrieve it from the config.json file.",
  "type": "object",
  "properties": {
    "url": {
      "$ref": "/base#/definitions/url"
    },
    "timeout": {
      "type": "number",
      "default": 10000
    }
  },
  "required": []
}