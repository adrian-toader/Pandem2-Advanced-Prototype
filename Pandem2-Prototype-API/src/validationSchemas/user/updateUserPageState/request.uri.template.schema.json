{
  "$id": "user.updateUserPageState.request.uri.template.schema",
  "description": "Update the userState object in the database",
  "type": "object",
  "properties": {
    "page": {
      "$ref": "/base#/definitions/userPageState"
    }
  },
  "required": [
    "page"
  ]
}